apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tmc.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "tmc.name" . }}
    helm.sh/chart: {{ include "tmc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tmc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
